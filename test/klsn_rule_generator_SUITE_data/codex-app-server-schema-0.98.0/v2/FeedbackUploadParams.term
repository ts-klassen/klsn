#{to_json =>
      {struct,#{reason => {optional,{any_of,[binstr,{exact,null}]}},
                threadId => {optional,{any_of,[binstr,{exact,null}]}},
                classification => {required,binstr},
                includeLogs => {required,boolean}}},
  from_json =>
      {struct,#{reason => {optional,{any_of,[binstr,{exact,null}]}},
                threadId => {optional,{any_of,[binstr,{exact,null}]}},
                classification => {required,binstr},
                includeLogs => {required,boolean}}}}.
