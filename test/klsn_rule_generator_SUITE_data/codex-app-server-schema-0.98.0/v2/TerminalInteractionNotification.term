#{to_json =>
      {struct,#{stdin => {required,binstr},
                threadId => {required,binstr},
                turnId => {required,binstr},
                itemId => {required,binstr},
                processId => {required,binstr}}},
  from_json =>
      {struct,#{stdin => {required,binstr},
                threadId => {required,binstr},
                turnId => {required,binstr},
                itemId => {required,binstr},
                processId => {required,binstr}}}}.
