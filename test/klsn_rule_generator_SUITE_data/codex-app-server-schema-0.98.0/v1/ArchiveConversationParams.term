#{to_json =>
      {with_defs,{#{<<"ThreadId">> => binstr},
                  {struct,#{conversationId => {required,{ref,<<"ThreadId">>}},
                            rolloutPath => {required,binstr}}}}},
  from_json =>
      {with_defs,{#{<<"ThreadId">> => binstr},
                  {struct,#{conversationId => {required,{ref,<<"ThreadId">>}},
                            rolloutPath => {required,binstr}}}}}}.
