{with_defs, {#{<<"RequestId">> => {any_of, [binstr, integer]}},
             {struct, #{id => {required, {ref, <<"RequestId">>}},
                        method => {required, binstr},
                        params => {optional, term}}}}}.
